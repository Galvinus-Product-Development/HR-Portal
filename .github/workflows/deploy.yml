# name: Deploy to Amazon ECR

# on:
#   push:
#     paths:
#       - "Backend-ED/**"
#       - "Backend-NS/**"
#       - "Backend-UM/**"
#       - "Backend-AS/**"
#       - "Backend-LM/**"
#       - "Backend-TL/**"
#       - "Frontend/**"
#     branches:
#       - dev

#   workflow_dispatch: # 🔹 Allows manual triggering

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4

#       - name: Configure AWS Credentials
#         uses: aws-actions/configure-aws-credentials@v2
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: us-east-1  # 🔹 Change this if needed

#       - name: Login to Amazon ECR
#         id: login-ecr
#         uses: aws-actions/amazon-ecr-login@v1

#       - name: Determine Changed Services
#         id: changed-files
#         uses: dorny/paths-filter@v2
#         with:
#           filters: |
#             Backend-ED:
#               - "Backend-ED/**"
#             Backend-NS:
#               - "Backend-NS/**"
#             Backend-UM:
#               - "Backend-UM/**"
#             Backend-AS:
#               - "Backend-AS/**"
#             Backend-LM:
#               - "Backend-LM/**"
#             Backend-TL:
#               - "Backend-TL/**"
#             Frontend:
#               - "Frontend/**"

#       - name: Ensure ECR Repository Exists for Backend-ED
#         if: steps.changed-files.outputs.Backend-ED == 'true'
#         run: |
#           aws ecr describe-repositories --repository-names backend-ed || \
#           aws ecr create-repository --repository-name backend-ed

#       - name: Build & Push Docker Image for Backend-ED
#         if: steps.changed-files.outputs.Backend-ED == 'true'
#         env:
#           ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
#           IMAGE_TAG: ${{ github.sha }}
#         run: |
#           docker build -t $ECR_REGISTRY/backend-ed:$IMAGE_TAG -f Backend-ED/Dockerfile Backend-ED
#           docker push $ECR_REGISTRY/backend-ed:$IMAGE_TAG

#       - name: Ensure ECR Repository Exists for Backend-NS
#         if: steps.changed-files.outputs.Backend-NS == 'true'
#         run: |
#           aws ecr describe-repositories --repository-names backend-ns || \
#           aws ecr create-repository --repository-name backend-ns

#       - name: Build & Push Docker Image for Backend-NS
#         if: steps.changed-files.outputs.Backend-NS == 'true'
#         env:
#           ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
#           IMAGE_TAG: ${{ github.sha }}
#         run: |
#           docker build -t $ECR_REGISTRY/backend-ns:$IMAGE_TAG -f Backend-NS/Dockerfile Backend-NS
#           docker push $ECR_REGISTRY/backend-ns:$IMAGE_TAG

#       - name: Ensure ECR Repository Exists for Backend-UM
#         if: steps.changed-files.outputs.Backend-UM == 'true'
#         run: |
#           aws ecr describe-repositories --repository-names backend-um || \
#           aws ecr create-repository --repository-name backend-um

#       - name: Build & Push Docker Image for Backend-UM
#         if: steps.changed-files.outputs.Backend-UM == 'true'
#         env:
#           ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
#           IMAGE_TAG: ${{ github.sha }}
#         run: |
#           docker build -t $ECR_REGISTRY/backend-um:$IMAGE_TAG -f Backend-UM/Dockerfile Backend-UM
#           docker push $ECR_REGISTRY/backend-um:$IMAGE_TAG

#       - name: Ensure ECR Repository Exists for Backend-AS
#         if: steps.changed-files.outputs.Backend-AS == 'true'
#         run: |
#           aws ecr describe-repositories --repository-names backend-as || \
#           aws ecr create-repository --repository-name backend-as

#       - name: Build & Push Docker Image for Backend-AS
#         if: steps.changed-files.outputs.Backend-AS == 'true'
#         env:
#           ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
#           IMAGE_TAG: ${{ github.sha }}
#         run: |
#           docker build -t $ECR_REGISTRY/backend-as:$IMAGE_TAG -f Backend-AS/Dockerfile Backend-AS
#           docker push $ECR_REGISTRY/backend-as:$IMAGE_TAG

#       - name: Ensure ECR Repository Exists for Backend-LM
#         if: steps.changed-files.outputs.Backend-LM == 'true'
#         run: |
#           aws ecr describe-repositories --repository-names backend-lm || \
#           aws ecr create-repository --repository-name backend-lm

#       - name: Build & Push Docker Image for Backend-LM
#         if: steps.changed-files.outputs.Backend-LM == 'true'
#         env:
#           ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
#           IMAGE_TAG: ${{ github.sha }}
#         run: |
#           docker build -t $ECR_REGISTRY/backend-lm:$IMAGE_TAG -f Backend-LM/Dockerfile Backend-LM
#           docker push $ECR_REGISTRY/backend-lm:$IMAGE_TAG

#       - name: Ensure ECR Repository Exists for Backend-TL
#         if: steps.changed-files.outputs.Backend-TL == 'true'
#         run: |
#           aws ecr describe-repositories --repository-names backend-tl || \
#           aws ecr create-repository --repository-name backend-tl

#       - name: Build & Push Docker Image for Backend-TL
#         if: steps.changed-files.outputs.Backend-TL == 'true'
#         env:
#           ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
#           IMAGE_TAG: ${{ github.sha }}
#         run: |
#           docker build -t $ECR_REGISTRY/backend-tl:$IMAGE_TAG -f Backend-TL/Dockerfile Backend-TL
#           docker push $ECR_REGISTRY/backend-tl:$IMAGE_TAG

#       - name: Ensure ECR Repository Exists for Frontend
#         if: steps.changed-files.outputs.Frontend == 'true'
#         run: |
#           aws ecr describe-repositories --repository-names frontend || \
#           aws ecr create-repository --repository-name frontend

#       - name: Build & Push Docker Image for Frontend
#         if: steps.changed-files.outputs.Frontend == 'true'
#         env:
#           ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
#           IMAGE_TAG: ${{ github.sha }}
#         run: |
#           docker build -t $ECR_REGISTRY/frontend:$IMAGE_TAG -f Frontend/Dockerfile Frontend
#           docker push $ECR_REGISTRY/frontend:$IMAGE_TAG


# name: Deploy to ECS

# on:
#   push:
#     paths:
#       - "Backend-ED/**"
#       - "Backend-NS/**"
#       - "Backend-UM/**"
#       - "Backend-AS/**"
#       - "Backend-LM/**"
#       - "Backend-TL/**"
#       - "Frontend/**"
#     branches:
#       - dev
#   workflow_dispatch:

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     env:
#       AWS_REGION: us-east-1
#       CLUSTER_NAME: ${{ secrets.CLUSTER_NAME }}
#       EXECUTION_ROLE_ARN: arn:aws:iam::<account-id>:role/ecsTaskExecutionRole
#       TASK_ROLE_ARN: arn:aws:iam::<account-id>:role/ecsTaskRole
#       SUBNETS: subnet-abc123,subnet-def456
#       SECURITY_GROUPS: sg-abc123

#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4

#       - name: Configure AWS Credentials
#         uses: aws-actions/configure-aws-credentials@v2
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: ${{ env.AWS_REGION }}

#       - name: Login to Amazon ECR
#         id: login-ecr
#         uses: aws-actions/amazon-ecr-login@v1

#       - name: Determine Changed Services
#         id: changed
#         uses: dorny/paths-filter@v2
#         with:
#           filters: |
#             Backend-ED: Backend-ED/**
#             Backend-NS: Backend-NS/**
#             Backend-UM: Backend-UM/**
#             Backend-AS: Backend-AS/**
#             Backend-LM: Backend-LM/**
#             Backend-TL: Backend-TL/**
#             Frontend: Frontend/**

#       - name: Deploy Microservices
#         if: always()
#         uses: actions/github-script@v7
#         with:
#           script: |
#             const ports = {
#               "backend-ed": 5001,
#               "backend-ns": 5002,
#               "backend-um": 5000,
#               "backend-as": 5003,
#               "backend-lm": 5005,
#               "backend-tl": 5004,
#               "frontend": 80
#             };

#             const services = {
#               "Backend-ED": "backend-ed",
#               "Backend-NS": "backend-ns",
#               "Backend-UM": "backend-um",
#               "Backend-AS": "backend-as",
#               "Backend-LM": "backend-lm",
#               "Backend-TL": "backend-tl",
#               "Frontend": "frontend"
#             };

#             const exec = require('child_process').execSync;
#             const fs = require('fs');
#             const ECR_REGISTRY = `${process.env.ECR_REGISTRY || '${{ steps.login-ecr.outputs.registry }}'}`;
#             const IMAGE_TAG = `${process.env.IMAGE_TAG || '${{ github.sha }}'}`;

#             for (const [folder, repo] of Object.entries(services)) {
#               if (process.env[`changed_${folder.replace('-', '_')}`] === 'true') {
#                 const serviceName = process.env[`SERVICE_NAME_${folder.replace('-', '_').toUpperCase()}`];
#                 const taskDefJson = `taskdef-${repo}.json`;
#                 const fullImage = `${ECR_REGISTRY}/${repo}:${IMAGE_TAG}`;

#                 console.log(`\u001b[1mDeploying ${repo}\u001b[0m`);

#                 exec(`aws ecr describe-repositories --repository-names ${repo} || aws ecr create-repository --repository-name ${repo}`);
#                 exec(`docker build -t ${fullImage} -f ${folder}/Dockerfile ${folder}`);
#                 exec(`docker push ${fullImage}`);

#                 const taskDef = {
#                   family: repo,
#                   networkMode: "awsvpc",
#                   requiresCompatibilities: ["EC2"],
#                   cpu: "256",
#                   memory: "512",
#                   executionRoleArn: process.env.EXECUTION_ROLE_ARN,
#                   taskRoleArn: process.env.TASK_ROLE_ARN,
#                   containerDefinitions: [
#                     {
#                       name: repo,
#                       image: fullImage,
#                       essential: true,
#                       portMappings: [
#                         {
#                           containerPort: ports[repo],
#                           protocol: "tcp"
#                         }
#                       ]
#                     }
#                   ]
#                 };

#                 fs.writeFileSync(taskDefJson, JSON.stringify(taskDef, null, 2));
#                 exec(`aws ecs register-task-definition --cli-input-json file://${taskDefJson}`);
#                 exec(`aws ecs update-service --cluster ${process.env.CLUSTER_NAME} --service ${serviceName} --force-new-deployment`);
#               }
#             }
#         env:
#           changed_Backend_ED: ${{ steps.changed.outputs.Backend-ED }}
#           changed_Backend_NS: ${{ steps.changed.outputs.Backend-NS }}
#           changed_Backend_UM: ${{ steps.changed.outputs.Backend-UM }}
#           changed_Backend_AS: ${{ steps.changed.outputs.Backend-AS }}
#           changed_Backend_LM: ${{ steps.changed.outputs.Backend-LM }}
#           changed_Backend_TL: ${{ steps.changed.outputs.Backend-TL }}
#           changed_Frontend: ${{ steps.changed.outputs.Frontend }}
#           SERVICE_NAME_BACKEND_ED: ${{ secrets.ED_SERVICE_NAME }}
#           SERVICE_NAME_BACKEND_NS: ${{ secrets.NS_SERVICE_NAME }}
#           SERVICE_NAME_BACKEND_UM: ${{ secrets.UM_SERVICE_NAME }}
#           SERVICE_NAME_BACKEND_AS: ${{ secrets.AS_SERVICE_NAME }}
#           SERVICE_NAME_BACKEND_LM: ${{ secrets.LM_SERVICE_NAME }}
#           SERVICE_NAME_BACKEND_TL: ${{ secrets.TL_SERVICE_NAME }}
#           SERVICE_NAME_FRONTEND: ${{ secrets.FRONTEND_SERVICE_NAME }}





name: Deploy to ECS

on:
  push:
    paths:
      - "Backend-UM/**"  # Only track changes in Backend-UM
    branches:
      - dev
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      AWS_REGION: us-east-1
      CLUSTER_NAME: ${{ secrets.CLUSTER_NAME }}
      EXECUTION_ROLE_ARN: arn:aws:iam::<account-id>:role/ecsTaskExecutionRole
      TASK_ROLE_ARN: arn:aws:iam::<account-id>:role/ecsTaskRole
      SUBNETS: subnet-abc123,subnet-def456
      SECURITY_GROUPS: sg-abc123

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1

      - name: Determine Changed Services
        id: changed
        uses: dorny/paths-filter@v2
        with:
          filters: |
            Backend-UM: Backend-UM/**  # Only track changes in Backend-UM

      - name: Deploy hr-app
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const ports = {
              "hr-app": 5000  # Adjusted port for hr-app service
            };

            const serviceName = "hr-app-service";  # Service name updated
            const repo = "hr-app";  # Repo name updated
            const exec = require('child_process').execSync;
            const fs = require('fs');
            const ECR_REGISTRY = process.env.ECR_REGISTRY || '${{ steps.login-ecr.outputs.registry }}';
            const IMAGE_TAG = process.env.IMAGE_TAG || '${{ github.sha }}';

            if (process.env.changed_Backend_UM === 'true') {
              const taskDefJson = `taskdef-${repo}.json`;
              const fullImage = `${ECR_REGISTRY}/${repo}:${IMAGE_TAG}`;

              console.log(`\u001b[1mDeploying ${repo}\u001b[0m`);

              exec(`aws ecr describe-repositories --repository-names ${repo} || aws ecr create-repository --repository-name ${repo}`);
              exec(`docker build -t ${fullImage} -f Backend-UM/Dockerfile Backend-UM`);
              exec(`docker push ${fullImage}`);

              const taskDef = {
                family: "hr-app-service",  # Task definition name updated
                networkMode: "awsvpc",
                requiresCompatibilities: ["EC2"],
                cpu: "256",
                memory: "512",
                executionRoleArn: process.env.EXECUTION_ROLE_ARN,
                taskRoleArn: process.env.TASK_ROLE_ARN,
                containerDefinitions: [
                  {
                    name: repo,
                    image: fullImage,
                    essential: true,
                    portMappings: [
                      {
                        containerPort: ports[repo],
                        protocol: "tcp"
                      }
                    ]
                  }
                ]
              };

              fs.writeFileSync(taskDefJson, JSON.stringify(taskDef, null, 2));
              exec(`aws ecs register-task-definition --cli-input-json file://${taskDefJson}`);
              exec(`aws ecs update-service --cluster ${process.env.CLUSTER_NAME} --service ${serviceName} --force-new-deployment`);
            }
        env:
          changed_Backend_UM: ${{ steps.changed.outputs.Backend-UM }}
